# 帧同步 (Lockstep) 通用框架 — 设计方案

## Context

从零构建一个 Unity 兼容的帧同步网络框架。目标是产出**纯 C# 核心库**，传输层可插拔（默认 TCP）、支持回放与观战。

**关键约束：**

- .NET Standard 2.0 API 面，不依赖 UnityEngine
- 所有数值计算使用定点数 (Q16.16)，禁止浮点
- 代码标识符英文，XML 文档注释中文

相关文档：`docs/实现进度.md` `docs/测试计划.md`

## 架构概览

```text
传输层 (Transport) ──→ 会话层 (Session) ──→ 仿真层 (Simulation)
                           │
                    ┌──────┼──────┐
                    ↓      ↓      ↓
               指令层   一致性层  回放/观战层
              (Command) (Consistency) (Replay/Spectator)

底层：定点数学库 (Math)
```

**数据流：** 玩家输入 → `CommandBuffer[frame]` → 收集所有玩家输入 → 广播 `FramePackage` → `IDeterministicGame.Update()`

## 模块清单

| 层 | 文件 | 职责 |
| --- | --- | --- |
| **Math** | `Fix64.cs` | Q16.16 定点数，含算术运算符、Lerp、Clamp、Sqrt |
| | `FixMath.cs` | 三角函数 (查表+线性插值)、Sqrt、常量 |
| | `FixVector2.cs` / `FixVector3.cs` | 2D/3D 定点向量 |
| | `FixMatrix.cs` | 3x3 变换矩阵 |
| **Core** | `LockstepConfig.cs` | 全局常量 |
| | `VarInt.cs` | 变长整数编解码 |
| | `ByteArrayPool.cs` | 网络缓冲池 |
| | `LogManager.cs` | 日志抽象 |
| **Command** | `IInputCommand.cs` | 游戏输入指令接口 |
| | `CommandBuffer.cs` | 线程安全的输入环形缓冲 |
| | `CommandSerializer.cs` | 二进制序列化 |
| | `CommandFactory.cs` | 指令类型注册与创建 |
| | `DeterministicInput.cs` | (PlayerId, Frame, Command) 结构体 |
| | `FramePackage.cs` | 单帧所有玩家输入的广播包 |
| **Transport** | `INetworkTransport.cs` | 传输层抽象接口 |
| | `IConnection.cs` | 连接句柄抽象 |
| | `TcpTransport.cs` | TCP 参考实现 |
| **Session** | `LockstepSession.cs` | **核心编排器**，协调所有层 |
| | `SessionConfig.cs` | 帧率、缓冲大小、超时、冗余度等配置 |
| | `SessionRole.cs` / `SessionState.cs` | 枚举 |
| | `FrameSynchronizer.cs` | 帧推进逻辑 + 动态缓冲调整 |
| | `PlayerConnection.cs` | 单玩家连接状态跟踪 |
| **Simulation** | `IDeterministicGame.cs` | 用户实现的确定性游戏逻辑接口 |
| | `IGameState.cs` | 游戏状态序列化接口 |
| | `GameLoop.cs` | Stopwatch 驱动的帧率控制器 |
| | `DeterministicRandom.cs` | 可种子化的伪随机数生成器 |
| **Consistency** | `ConsistencyChecker.cs` | 每 K 帧交换哈希并比对 |
| | `DesyncInfo.cs` | 不一致事件数据 |
| **Replay** | `ReplayRecorder.cs` / `ReplayPlayer.cs` | 录制/播放指令流 |
| | `ReplayMetadata.cs` | 回放文件头 |
| **Spectator** | `SpectatorSession.cs` | 迟加入观战会话 |

## 关键设计决策

| # | 决策点 | 选择 | 理由 |
| --- | --- | --- | --- |
| 1 | 帧缓冲策略 | 默认 3 帧，可选动态调整 (2~6) | 均衡延迟与抗抖动 |
| 2 | 输入冗余 | 每包携带最近 3 帧输入 | 接收端 (PlayerId, Frame) 去重，抗丢包 |
| 3 | 一致性校验 | 每 10 帧 CRC64 哈希广播比对 | 3 次/秒 足够检测，开销低 |
| 4 | 状态快照 | 每 30 帧保存，环形缓冲 64 条 | 支持观战重连，容量覆盖约 2 秒 |
| 5 | 定点数格式 | Q16.16 | 乘积极限 2^62 < 2^63，无 128 位溢出风险 |
| 6 | 指令序列化 | VarInt 紧凑二进制 (类 ProtoBuf) | 小值高效，帧号等通常 1-2 字节 |
| 7 | 掉线处理 | 可配策略：等待全部 / 空指令 / 中止 | 不同游戏容忍度不同 |
| 8 | 三角函数 | 4096 条目查表 + 线性插值 | 确定 + 快，精度 ~1.5e-4 |

## 核心接口

### INetworkTransport

```csharp
/// <summary>网络传输层抽象接口。支持插件式替换 (TCP/UDP)</summary>
public interface INetworkTransport
{
    event Action<IConnection> OnConnected;
    event Action<IConnection, string> OnDisconnected;
    event Action<IConnection, ArraySegment<byte>> OnDataReceived;

    void StartServer(int port, int maxConnections);
    void Connect(string host, int port);
    void Send(IConnection conn, ArraySegment<byte> data, SendOptions opts);
    void Broadcast(ArraySegment<byte> data, SendOptions opts, IConnection exclude = null);
    void Update();
    void Shutdown();

    int ConnectionCount { get; }
    bool IsServer { get; }
}
```

### IDeterministicGame

```csharp
/// <summary>确定性游戏逻辑接口。相同输入必须产生完全相同的结果</summary>
public interface IDeterministicGame
{
    string GameId { get; }
    int GameVersion { get; }

    void Initialize(IGameState initialState);
    void Update(DeterministicInput[] inputs, int frame);
    ulong GetFrameHash();
    IGameState GetStateSnapshot();
    void RestoreState(IGameState state);
}
```

### LockstepSession

```csharp
/// <summary>帧同步会话核心编排器</summary>
public class LockstepSession : IDisposable
{
    event Action<int> OnFrameAdvanced;
    event Action<SessionState> OnStateChanged;
    event Action<DesyncInfo> OnDesyncDetected;
    event Action<int, string> OnPlayerDisconnected;

    void Start(int localPlayerId, SessionRole role, int playerCount = -1);
    void Stop();
    void Update();
    void SubmitInput(IInputCommand command);
    void ForceAdvanceFrame();

    int CurrentFrame { get; }
    SessionState State { get; }
    void AttachReplaySource(ReplayPlayer replay);
    void EnableSpectatorSupport(SpectatorConfig config = null);
}
```
